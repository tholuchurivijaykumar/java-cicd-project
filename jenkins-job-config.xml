<?xml version='1.1' encoding='UTF-8'?>
<flow-definition plugin="workflow-job">
  <description>Java CI/CD Pipeline - Build, Test, Docker Push, Kubernetes Deploy</description>
  <keepDependencies>false</keepDependencies>
  <properties>
    <org.jenkinsci.plugins.workflow.job.properties.PipelineTriggersJobProperty>
      <triggers>
        <hudson.triggers.TimerTrigger>
          <spec>H/2 * * * *</spec>
        </hudson.triggers.TimerTrigger>
      </triggers>
    </org.jenkinsci.plugins.workflow.job.properties.PipelineTriggersJobProperty>
  </properties>
  <definition class="org.jenkinsci.plugins.workflow.cps.CpsFlowDefinition" plugin="workflow-cps">
    <script><![CDATA[
pipeline {
    agent any

    environment {
        DOCKER Hub_CREDENTIALS = credentials('dockerhub-creds')
        DOCKER Hub_IMAGE = 'vijay14082003/java-cicd-app'
        DOCKER TAG = "${env.BUILD_NUMBER}"
    }

    triggers {
        cron('H/2 * * * *')
    }

    stages {
        stage('Checkout') {
            steps {
                git branch: 'main',
                    url: 'https://github.com/tholuchurivijaykumar/java-cicd-project.git',
                    credentialsId: 'github-creds'
            }
        }

        stage('Build') {
            steps {
                sh 'mvn clean compile -B'
            }
        }

        stage('Unit Tests') {
            steps {
                sh 'mvn test -B'
            }
            post {
                always {
                    junit '**/target/surefire-reports/*.xml'
                }
            }
        }

        stage('Package') {
            steps {
                sh 'mvn package -DskipTests -B'
            }
        }

        stage('Docker Build and Push') {
            steps {
                script {
                    sh "docker build -t ${DOCKER Hub_IMAGE}:${DOCKER TAG} ."
                    sh "docker tag ${DOCKER Hub_IMAGE}:${DOCKER TAG} ${DOCKER Hub_IMAGE}:latest"
                    sh "echo ${DOCKER Hub_CREDENTIALS_PSW} | docker login -u ${DOCKER Hub_CREDENTIALS_USR} --password-stdin"
                    sh "docker push ${DOCKER Hub_IMAGE}:${DOCKER TAG}"
                    sh "docker push ${DOCKER Hub_IMAGE}:latest"
                }
            }
        }

        stage('Deploy to Minikube') {
            steps {
                script {
                    sh "kubectl set image deployment/java-cicd-app java-cicd-app=${DOCKER Hub_IMAGE}:${DOCKER TAG} --record"
                    sh "kubectl rollout status deployment/java-cicd-app --timeout=120s"
                    sh "kubectl get pods -l app=java-cicd-app"
                    sh "kubectl get svc java-cicd-app-service"
                }
            }
        }

        stage('Verify Deployment') {
            steps {
                script {
                    sh "kubectl get all | grep java-cicd-app"
                    sh "curl -s http://localhost:\$(kubectl get svc java-cicd-app-service -o jsonpath='{.spec.ports[0].nodePort}')/api/hello || echo 'App starting up...'"
                }
            }
        }
    }

    post {
        success {
            echo 'Pipeline completed successfully! Application deployed to Minikube.'
        }
        failure {
            echo 'Pipeline failed. Check logs for details.'
        }
        always {
            sh 'docker logout || true'
        }
    }
}
]]></script>
    <sandbox>true</sandbox>
  </definition>
  <triggers/>
  <disabled>false</disabled>
</flow-definition>
